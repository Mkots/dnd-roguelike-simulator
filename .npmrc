# Fail the install if the lockfile is out of sync with package.json.
# Always use --frozen-lockfile in CI explicitly, but this is a local safety net.
frozen-lockfile=false

# Do not hoist packages to the root node_modules — keeps the dependency graph honest.
shamefully-hoist=false
